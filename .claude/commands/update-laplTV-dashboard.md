# 라플TV 캠페인 대시보드 데일리 업데이트

이 스킬을 실행하면 최신 엑셀 보고서를 읽어 `라플TV_캠페인_대시보드.html`을 업데이트한다.

---

## 핵심 개념: 날짜 오프셋 (2026-07-06부터 규칙 변경)

**2026-07-06 이후 파일(예: `..._260705.xlsx`)부터: 오프셋 없음.**
엑셀 파일명의 날짜 = 마감일자(실제 광고 집행 마지막 날) 그 자체. `260705.xlsx` → HTML `'7/5'`까지 그대로 반영.
`update_dashboard.ps1`도 이에 맞춰 47번 줄의 `$endDay = ... - 1`에서 `- 1`을 제거함 (2026-07-06 수정).

**2026-07-05 이전 파일은 아래의 구(舊) 1일 오프셋 규칙이 적용됐었다** (과거 리포트 데이터를 다시 볼 일이 있을 때만 참고):

> 엑셀의 날짜 = 광고 데이터 보고 기준일 (= 실제 광고 집행일 + 1일)
> HTML 레이블 = 실제 광고 집행일
> 따라서: HTML '6/N' = Excel (N+1)일 행
> 예) 엑셀 6월 25일(46197) 행 → HTML `'6/24'` 레이블로 입력

**Why:** 광고 대행사/데이터 제공처가 엑셀 파일명에 넣는 날짜의 의미를 "생성일(익일)"에서 "마감일자(당일)"로 변경했다고 사용자가 확인함. 새 파일이 오면 항상 최근 파일이 신규칙, 과거 파일은 구규칙이라는 걸 헷갈리지 말 것 — 파일명만 보고 규칙을 추측하지 말고, 이 문서의 최신 날짜 이후 파일인지부터 확인.

---

## ⚠️ 필수 주의사항

### replace_all 사용 절대 금지

HTML 내 `var data=[{lbl:'6/XX',...}]` 패턴이 여러 차트에 동시에 존재한다.  
`replace_all`로 날짜 배열을 교체하면 **의도하지 않은 다른 차트 데이터도 함께 덮어써진다**.

예: 전환 롤링 7일 차트(`cv1`)와 클릭최대화 프로그램별 차트(`clickProg`)가 동일 패턴 공유  
→ `replace_all` 시 `clickProg` 데이터가 전환 데이터로 교체됨 → `d.c` 속성 없음 → 차트 미표시

**항상 충분한 주변 컨텍스트로 단건 교체(replace_all:false)할 것.**

### CC 객체 새 날짜 추가 시 버그 주의

CC 객체(`전환 소재별`)에 새 날짜를 추가할 때, **직전 날짜 값이 그대로 복사되는 버그**가 발생한 이력이 있다.  
새 날짜 추가 후 반드시 직전 날짜 값과 비교 검증할 것 (군체/토이스토리/스필버그 3개 소재 모두).

---

## 🗓️ 월 전환 시 필수 체크리스트 (매월 1회, 매우 중요)

> 2026-07 전환 시 클릭최대화·전환·후속조회 3개 탭에서 KPI·프로그램별 성과·각종 차트·소재별
> 테이블·일자별 상세보기가 동시다발적으로 틀어져 있던 걸 발견해서 정리한 것.
> **근본 원인은 하나다**: 월이 바뀌면서 새로 시작한 소재(회차)가 있었는데, 기존에 알던 소재
> 이름만 계속 갱신하고 "엑셀에 새 소재 블록이 생겼는지"를 확인하지 않아서 그 소재 데이터가
> 통째로 누락됐다. 그 결과 프로그램별 합계가 소재별 합계와 안 맞고, 여러 차트/테이블이 전달
> 값 그대로 남아있었다. **다음 달 전환 때는 아래 A~G를 순서대로 확인할 것.**

### A. 신규 소재 탐지 — 가장 먼저 할 일

"이미 아는 소재 이름 목록"만 기준으로 값을 갱신하지 말고, **엑셀 소재별 데이터 섹션의 헤더
행 전체를 매번 다시 스캔**해서 새로 생긴 소재 블록이 있는지 확인한다.

```powershell
# 디맨드젠_클릭최대화 시트 예시 — 소재별 데이터 블록은 10칸 간격(B,L,V,AF,AP,AZ...)으로 반복
$blockStarts = 2,12,22,32,42,52   # 시트/월마다 개수·위치가 달라질 수 있음
for ($r=1; $r -le $used.Rows.Count; $r++) {
  foreach ($bs in $blockStarts) {
    $nameCell = $vals[$r,$bs]
    if ($nameCell -is [string] -and $vals[$r+1,$bs] -eq '기간') {
      Write-Output "HEADER row=$r col=$bs name=$nameCell"
    }
  }
}
```

이렇게 뽑은 전체 헤더 목록을 **JUNE/CC/RD/CREATIVES 등 HTML 쪽 객체에 이미 있는 소재 이름과
대조**한다. 목록엔 있는데 HTML엔 없으면 신규 소재 → 반드시 추가.

**2026-07 실사례**: 디맨드젠_클릭최대화 시트에 뮤지컬1(20화)·뮤지컬2(21화)·더베어(HOT17화)·
뉴뮤지엄(딥7화)·장송의프리렌(딥8화) 5개 신규 소재 블록이 이미 있었는데, JUNE 객체는 스필버그·
토이스토리(13화)만 계속 갱신하고 있어서 5개 전부 누락된 채로 며칠이 지났다. 이 때문에 KPI
노출/클릭/CPC/CPV가 전부 틀어져 있었다.

### B. "합계" 행을 맹신하지 말 것 — 신규 소재 컬럼의 스프레드시트 복사 버그

신규 소재 컬럼은 기존 소재(주로 직전 회차)의 수식·서식을 복사해서 만드는 경우가 많다. 그 결과
**신규 소재가 실제로 시작하기 전 구간에는 옆 컬럼(또는 이전 회차)의 값이 그대로 찍혀 있는
경우**가 있다. 2026-07 사례에서는 뮤지컬1/뮤지컬2/토이스토리(HOT16화) 3개 컬럼이 6/23~6/30
구간에 완전히 동일한 값을 보였다 — 실제로는 뮤지컬1/2가 그 기간에 존재하지 않았는데 옆 컬럼
(토이스토리 HOT16화)의 실제 값이 그대로 복사되어 있었던 것. 심지어 "합계" 라벨이 붙은 요약
행 하나도 3개 컬럼 모두 동일한 값을 보여줘서, 요약 행만 보고는 이 버그를 알아챌 수 없었다.

**대응**: 신규 소재가 시작한 것으로 보이는 날짜 이전 구간에서 인접한 여러 소재 컬럼 값이
완전히 똑같다면 그 구간은 버리고, **다른 컬럼과 값이 달라지기 시작하는 첫 날**부터만 진짜
데이터로 취급한다. 반드시 raw 셀을 여러 개 나란히 찍어서 눈으로 비교해 판단할 것.

### C. 요일 계산은 항상 `new Date(2026, month-1, day).getDay()` — `day % 7` 금지

`day % 7` 같은 하드코딩 패턴은 "이번 달 1일이 무슨 요일인가"에 따라 옵셋이 달마다 달라지므로
월이 바뀌면 반드시 틀린다. 2026-07 전환 때 클릭최대화/전환/후속조회 3개 탭의 "일자별 OOO
현황" 섹션과 개요 탭 `cv1` 차트에서 전부 이 패턴이 쓰이고 있었고, 7월 데이터를 추가하자마자
요일이 실제 달력과 어긋나는 버그가 반복 발생했다.

```javascript
// 나쁜 예 (월 경계에서 깨짐)
var dowStr = ['일','월','화','수','목','금','토'][day % 7];

// 좋은 예 (항상 정확 — 2026년 기준, month는 1~12)
var dowStr = ['일','월','화','수','목','금','토'][new Date(2026, month-1, day).getDay()];
```

### D. "일자별 OOO 현황" 3개 탭 공통 패턴 — 월 구분 라벨 + 신규 월 버튼 추가

클릭최대화(`click-day-btns`)·전환(`conv-day-btns`)·후속조회(`retarget-day-btns`) 3개 탭 모두
구조가 동일하다. 월이 바뀔 때마다 아래를 그대로 적용한다:

1. 버튼 목록 앞에 이전 달 라벨(`6월`)을 넣고 이전 달 버튼(1~30/31)을 렌더링
2. 그 뒤에 새 달 라벨(`7월`)을 넣고 새 달 버튼(1~보고서기준일)을 렌더링
3. `data-date`/`data-cdate`/`data-rdate` 값은 `"7/N"` 형식으로 새 달 버튼에만 부여
4. 기본 선택(`on` 클래스)과 초기 렌더 호출(`renderDateCards('7/N')` 등)을 **최신 날짜**로 변경
5. 렌더 함수 내부에서 `key.split('/')`로 **월도 함께 파싱**해서 노트 텍스트의 "N월"과 요일
   계산(C번 항목)에 반영 — 이걸 빠뜨리면 `mo is not defined` 런타임 에러가 난다
6. 섹션 타이틀(`.sect-lbl`)도 "· 7월" → "· 6월~7월"로 바꿔 두 달치가 섞여 있음을 표시

```javascript
// 버튼 렌더링 템플릿 (세 탭 공통, 신규월마지막일 = 보고서 기준일)
var h='<span style="font-size:11px;color:var(--faint);align-self:center;letter-spacing:1px;margin-right:2px;">6월</span>';
for(var d2=1;d2<=30;d2++){
  var isWk=['일','토'].indexOf(['일','월','화','수','목','금','토'][new Date(2026,5,d2).getDay()])>=0;
  h+='<button class="date-btn'+(isWk?' wk':'')+'" data-date="6/'+d2+'">'+d2+'</button>';
}
h+='<span style="font-size:11px;color:var(--faint);align-self:center;letter-spacing:1px;margin:0 2px 0 10px;">7월</span>';
for(var d3=1;d3<=신규월마지막일;d3++){
  var isWk3=['일','토'].indexOf(['일','월','화','수','목','금','토'][new Date(2026,6,d3).getDay()])>=0;
  var cls3='date-btn'+(isWk3?' wk':'')+(d3===신규월마지막일?' on':'');
  h+='<button class="'+cls3+'" data-date="7/'+d3+'">'+d3+'</button>';
}
```

### E. 차트별 "당월만" vs "월별 누적 유지" 규칙 (2026-07 확정)

같은 "일별 추이" 류 차트라도 성격이 다르다. 2026-07 전환 때 사용자가 명시적으로 확정한 규칙:

| 차트 | 규칙 | 이유 |
|------|------|------|
| `cv1` (개요, 일별 구독전환수) | **당월만** | 7일 롤링 차트라 다음 달이면 어차피 새로 채워짐 |
| `convDaily` (전환, 일별 구독전환수&CVR) | **당월만** | 사용자가 "6월 제외, 7월 실적만" 명시적으로 요청 |
| `retargetDaily` (후속조회, 일별 조회수&CTR) | **당월만** | 위와 동일 + 미집행이면 0으로 표시해 미집행 상태를 그대로 보여줌 |
| `clickTrend` (클릭최대화, 월별 CTR 추이) | **월별 누적 유지, 새 달 추가만** | 제목 자체가 "월별" 추이 — 여러 달 비교가 목적 |
| "일자별 OOO 현황" 버튼+카드 (3개 탭) | **이전 달 + 당월 모두, 월 구분 라벨로 표시** | 일 단위 상세 조회 기능 — 과거 날짜도 계속 조회 가능해야 함 |

표에 없는 새 케이스면 차트 제목에 기간이 괄호로 명시돼 있는지 먼저 본다 (`(7/1~7/5)`처럼
써 있으면 그 기간만 표시하는 게 기본값). 애매하면 사용자에게 먼저 확인할 것.

### F. 프로그램별 합계 = 소재별 합계 교차검증 (필수)

KPI 박스·프로그램별 성과 카드 숫자는 반드시 **소재별로 갱신한 데이터를 프로그램 단위로
합산한 값과 일치**해야 한다. 엑셀에 프로그램별 합계 표가 이미 있으면(예: 디맨드젠_클릭최대화
시트 맨 위 **"1. SUMMARY"** 표 — R104 소재별 합계를 일일이 더할 필요 없이 프로그램별 광고비·
노출·조회·클릭·CTR·CPV가 바로 나와 있다) 그 표 값을 그대로 쓰되, HTML에 반영한 소재별
데이터의 합과 반드시 대조한다.

```bash
node -e "
const fs = require('fs');
const html = fs.readFileSync('라플TV_캠페인_대시보드.html', 'utf8');
const m = html.match(/var JUNE=\{([\s\S]*?)\n\};/);
const JUNE = eval('({' + m[1] + '})');
let progCost={weekly:0,hot:0,deep:0};
['7/1','7/2','7/3','7/4','7/5'].forEach(k=>{
  JUNE[k].forEach(c=>{ progCost[c.prog]+=c.cost; });
});
console.log(progCost);  // 엑셀 '1. SUMMARY' 표의 프로그램별 광고비와 비교
"
```

이 방식으로 2026-07 사례의 모든 숫자(라플위클리 669,757원, HOT&NEW 1,433,880원, 딥다이브
289,285원 등)를 소수점 오차 수준까지 검증했다. **주의**: 처음 clickProg 차트를 고칠 때 JUNE
객체가 아직 불완전한 상태에서(A번 버그 발견 전) 계산해서, 딥다이브 몫을 HOT&NEW로 잘못
배정한 채 한 번 배포한 적이 있다 — 반드시 A번(신규 소재 탐지)을 먼저 끝내고 나서 이 교차검증을
해야 숫자가 맞는다.

### G-1. 서술형 문구의 날짜 범위는 가급적 노출하지 않는다 (2026-07-07 확정)

"월간 주요 인사이트" 같은 서술형 요약 타이틀·문구에 `7월 1일~5일`처럼 구체적 날짜 범위를
박아두면, 매일 갱신 대상이 하나 더 늘어날 뿐 아니라 자동 스크립트가 안 건드리는 영역이라
계속 하루 이틀씩 뒤처진 채로 방치되기 쉽다(실제로 여러 번 발생). 차트 제목처럼 **데이터
범위를 정확히 알아야 하는 곳**(`(7/1~7/5)` 등)이 아니라면, 서술형 타이틀/코멘트에서는
날짜 범위를 아예 빼고 "월 종합 분석"처럼 쓰는 쪽을 기본값으로 한다. "N일 경과" 같은
경과일수 표현도 마찬가지로 뺀다.

### G. 캠페인이 "미집행" 상태일 때 표기 컨벤션

특정 캠페인/프로그램이 해당 월에 전혀 집행되지 않았으면 (예: 2026-07 후속조회 캠페인):

- KPI/프로그램카드 배지: `<span class="badge" style="background:rgba(255,71,87,.12);color:var(--red);">미집행</span>`
- 카드 전체에 `opacity:.6`을 추가해 시각적으로 비활성 느낌을 준다
- 수치는 `0`, 비율(CTR/CVR 등)은 `-`로 표기 (0%로 쓰면 "집행은 했는데 성과가 0%"처럼 오해될 수 있음)
- 소재별 테이블에서 직전까지 활성이던 소재는 "활성"→"중단"으로 상태값을 바꾸고 운영기간에
  종료일(보통 전월 말일)을 명시, 해당 월 실적 컬럼은 0/`-`로 정리
- "일별 추이" 차트는 E번 규칙대로 당월 구간만 만들고 값은 전부 0으로 — 차트 자체를 숨기지
  않는다 (0이 찍힌 빈 차트가 "미집행"이라는 사실을 가장 명확하게 보여준다)

---

## 0단계 — 파일 확인

작업 디렉토리: 현재 Claude Code 작업 디렉토리 (`$pwd`)

**대시보드 파일**: `라플TV_캠페인_대시보드.html`

**최신 엑셀 보고서**: 아래 PowerShell로 자동 감지한다.

```powershell
$projRoot = (Get-Location).Path
Get-ChildItem $projRoot -Filter "차이_한화_라이프플러스_라플TV_Report_*.xlsx" |
  Sort-Object LastWriteTime -Descending | Select-Object -First 1 -ExpandProperty FullName
```

파일명의 날짜(예: `260705` = 2026년 07월 05일)를 확인해 **보고서 기준일**을 파악한다.  
**2026-07-06 이후 파일**: HTML에 표시할 날짜 = 보고서 기준일 그대로 (예: `260705` → `7/5`). 오프셋 없음.  
**2026-07-05 이전 파일(과거 데이터 참고 시만)**: HTML에 표시할 날짜 = 보고서 기준일 - 1일 (예: `260625` → `6/24`).

**엑셀 비밀번호**: 팀 내부 공유 비밀번호 사용 (`$xlPassword` 변수로 전달, 기본값 `"1234"`)

---

## 1단계 — 현재 HTML 최신 날짜 확인

HTML에서 각 객체의 마지막 날짜 항목을 Grep으로 확인한다.

```
var JUNE = { ... '6/XX':[ ... ]}   → 클릭최대화 소재별 마지막 날짜
var RD = { ... '6/XX':{ ... }}     → 후속조회 일별 마지막 날짜
var RC = { ... '6/XX':[ ... ]}     → 후속조회 소재별 마지막 날짜
var CONV = { ... '6/XX':{ ... }}   → 전환 일별 마지막 날짜
var CC = { ... '6/XX':[ ... ]}     → 전환 소재별 마지막 날짜
```

**업데이트 필요 범위**: 마지막 HTML 날짜 다음 날부터 `(보고서기준일 - 1일)`까지

---

## 2단계 — 엑셀 전체 데이터 추출

아래 PowerShell 패턴으로 **단일 명령**에서 모든 시트를 읽는다 (세션 분리 불가).

```powershell
$xl = New-Object -ComObject Excel.Application
$xl.Visible = $false; $xl.DisplayAlerts = $false
$xlPassword = "1234"   # 팀 내부 비밀번호로 교체
$wb = $xl.Workbooks.Open($xlPath, 0, $true, 5, $xlPassword)

$wsTotal   = $wb.Sheets.Item("Grand total")
$wsClick   = $wb.Sheets.Item("디맨드젠_클릭최대화")
$wsRetgt   = $wb.Sheets.Item("디맨드젠_후속조회")
$wsConv    = $wb.Sheets.Item("디맨드젠_전환캠페인")
$wsHot     = $wb.Sheets.Item("HOT & NEW_조회")
$wsWeekly  = $wb.Sheets.Item("라플위클리_조회")
$wsDeep    = $wb.Sheets.Item("딥다이브_조회")

# ... 아래 단계별로 데이터 읽기 ...

$wb.Close($false); $xl.Quit()
```

### 엑셀 날짜 시리얼 → 실제 날짜 변환
- 46173 = 2026년 6월 1일 (일)
- 46196 = 2026년 6월 24일 (화)
- 계산: `날짜 = 46173 + (실제일 - 1)`

---

## 3단계 — KPI 박스 업데이트 (5개 탭 전체)

> 각 탭 상단의 성과 요약 박스. 엑셀 **6월합계 행**에서 읽는다. 매일 전체 갱신.

### 엑셀 6월합계 행 위치 (확인됨)

| 시트 | 6월합계 행 | 주요 열 |
|------|-----------|---------|
| 디맨드젠_후속조회 (일별총합) | R19 | C4=비용, C5=노출, C6=조회, C7=클릭, C9=CTR |
| 디맨드젠_후속조회 (소재별) | R55 | C4=토이스토리비용, C14=실수비용, C24=스필버그비용 |
| 디맨드젠_전환캠페인 (일별총합) | R184 | C4=비용, C5=노출, C6=조회, C7=클릭, C9=CTR, C12=전환건수, C13=CVR |
| 디맨드젠_전환캠페인 (소재별) | R377 | C4=군체비용, C14=스필버그비용, C24=토이스토리비용 |
| 디맨드젠_클릭최대화 (소재별) | R104 | C4=스필버그비용, C5=노출, C6=조회, C7=클릭, C9=CTR, C10=CPC |
|  | | C14=토이스토리비용, C15=노출, C16=조회, C17=클릭, C19=CTR, C20=CPC |
|  | | C24=HOT&NEW비용, C25=노출, C26=조회, C27=클릭, C29=CTR, C30=CPC |
| Grand total | R16 (유튜브 subtotal) | C6=노출, C7=조회, C8=클릭, C12=소진금액(VAT+) |

### 예산 고정값 — ⚠️ 매월 재확인 필요 (월별로 재편성됨)

예산은 "고정값"이 아니라 **매월 초 캠페인별로 재편성**된다. 아래는 확인된 시점 기준 참고값이고,
새 달이 시작되면 Grand total 시트 또는 캠페인별 예산 셀에서 반드시 다시 확인할 것.

| 캠페인 | 2026-06 예산 | 2026-07 예산 |
|--------|------|------|
| 조회캠페인 (유튜브만) | 3,983만원 | 3,100만원 |
| 클릭최대화 | 1,000만원 | 1,500만원 |
| 후속조회 | 1,000만원 | 500만원 (7월 미집행) |
| 전환 | 1,000만원 | 2,500만원 |
| **전체** | **6,983만원** | **7,600만원** |

**Why:** 2026-07 전환 때 예산이 전부 재편성됐는데, 소진율(%) 계산에 구예산을 그대로 쓰면
100%를 훌쩍 넘는 이상한 숫자가 나온다. 소진율%는 항상 `해당 월 실제 예산` 기준으로 계산할 것.

---

### 3-1. 개요 탭 KPI

**HTML 위치**: `id="page-overview"` → 첫 번째 `.kpi-grid` (6개 박스)

**데이터 소스**: 4개 캠페인 KPI 합산

```
총 광고비 = 조회집행액 + 클릭집행액 + 후속집행액 + 전환집행액  (만원단위)
소진율 = round(총광고비 / 6983 * 100)%
총 노출 = Grand total R16 C6
총 조회 = Grand total R16 C7
총 클릭 = Grand total R16 C8
CTR = round(총클릭/총노출*100, 2)%
CPV = round(총광고비×10000/총조회)원
CPC = round(총광고비×10000/총클릭)원
전환 = $wsConv.Cells(184,12)건
구독단가 = round($wsConv.Cells(184,4)/$wsConv.Cells(184,12))원
```

**HTML 업데이트 대상** (class="kpi a/t" 박스):
```html
<div class="kpi a">총광고비만원 / 예산 6,983만 · X% 소진</div>
<div class="kpi">총노출만회 / X,XXX,XXX회</div>
<div class="kpi t">총조회만 / X,XXX,XXX회</div>
<div class="kpi t">X.XX% CTR / 클릭 XXX,XXX</div>
<div class="kpi">CPV XX원 / CPC XXX원</div>
<div class="kpi t">X,XXX건 전환 / 단가 X,XXX원</div>
```

---

### 3-2. 조회캠페인 탭 KPI

**HTML 위치**: `id="page-view"` → `.kpi-grid` (5개 박스)

**데이터 소스**: HOT&NEW_조회 + 라플위클리_조회 + 딥다이브_조회 시트 6월합계 합산

**컬러 클래스 규칙**:
- 소진금액 → `kpi a` (orange)
- 총 조회수 → `kpi t` (teal)
- 평균 CPV → `kpi t` (teal)

> ⚠️ **"조회캠페인" 소진율에 디맨드젠(클릭최대화/후속조회/전환) 비용을 섞지 말 것.** 조회캠페인
> 예산(3,100만원, 2026-07 기준)은 라플위클리·HOT&NEW·딥다이브 3개 조회 캠페인만의 예산이다.
> "월간 주요 인사이트" 섹션 등 서술형 텍스트에서 "조회캠페인 전체(디맨드젠 포함)"처럼 두 개념을
> 섞어 쓰면 이 탭 KPI 박스의 소진율(%)과 다른 숫자를 암시하게 되어 혼란을 준다. 디맨드젠 4개
> 캠페인은 각자 탭에 이미 별도 소진율이 있으므로, 조회캠페인 관련 문구는 항상 이 3개 조회
> 캠페인 합산 기준으로만 작성한다.

---

### 3-3. 클릭최대화 탭 KPI

**HTML 위치**: `id="page-click"` → `.kpi-grid` (6개 박스)

> ⚠️ 아래 R104 합산 대신, 시트 맨 위 **"1. SUMMARY"** 표(프로그램별 광고비/노출/조회/클릭/CTR/CPV가
> 이미 계산되어 있음)를 먼저 확인하는 게 더 빠르고 안전하다. 단, 그 값을 쓰기 전에 반드시
> [체크리스트 A번](#-월-전환-시-필수-체크리스트-매월-1회-매우-중요)으로 신규 소재 누락부터 확인할 것 —
> SUMMARY 표 자체는 정확해도, 소재별 상세(JUNE 등)에 신규 소재를 안 채워 넣으면 프로그램별 성과
> 카드·소재별 테이블·각종 차트는 여전히 틀린 채로 남는다.

**데이터 소스**: `$wsClick` R104 소재별 6월합계 합산

```powershell
$ck_cost  = $wsClick.Cells(104,4)  + $wsClick.Cells(104,14) + $wsClick.Cells(104,24)  # 총 소진금액
$ck_imp   = $wsClick.Cells(104,5)  + $wsClick.Cells(104,15) + $wsClick.Cells(104,25)  # 총 노출
$ck_views = $wsClick.Cells(104,6)  + $wsClick.Cells(104,16) + $wsClick.Cells(104,26)  # 총 조회
$ck_click = $wsClick.Cells(104,7)  + $wsClick.Cells(104,17) + $wsClick.Cells(104,27)  # 총 클릭
$ck_ctr   = [Math]::Round($ck_click / $ck_imp * 100, 2)                               # CTR
$ck_cpc   = [Math]::Round($ck_cost / $ck_click)                                        # CPC
$ck_cpv   = [Math]::Round($ck_cost / $ck_views)                                        # CPV
$ck_pct   = [Math]::Round($ck_cost / 100000000 * 100)                                  # 소진율(%)
```

**컬러 클래스 규칙**:
- 6월 집행액 → `kpi a` (orange)
- 총 클릭수 → `kpi t` (teal)
- 평균 CPC → `kpi a` (orange)
- 총 조회수 → `kpi t` (teal)

---

### 3-4. 후속조회 탭 KPI

**HTML 위치**: `id="page-retarget"` → `.kpi-grid` (5개 박스)  
**레이블**: `후속 조회 캠페인 · 6월 성과 (6/1~6/N)`

**데이터 소스**: `$wsRetgt` R19 (일별총합 6월합계)

```powershell
$rt_cost  = $wsRetgt.Cells(19,4)                                # 총 소진금액
$rt_imp   = $wsRetgt.Cells(19,5)                                # 총 노출
$rt_views = $wsRetgt.Cells(19,6)                                # 총 조회
$rt_click = $wsRetgt.Cells(19,7)                                # 총 클릭
$rt_ctr   = [Math]::Round($wsRetgt.Cells(19,9)*100, 2)          # CTR
$rt_cpv   = [Math]::Round($rt_cost / $rt_views)                 # CPV
$rt_cpc   = [Math]::Round($rt_cost / $rt_click)                 # CPC
$rt_pct   = [Math]::Round($rt_cost / 100000000 * 100)           # 소진율(%)
$rt_costM = [Math]::Round($rt_cost / 10000)                     # 만원
```

**HTML 업데이트 대상**:
```html
<div class="kpi a">Xman원 / 예산 1,000만 · X% 소진</div>
<div class="kpi">XXX만 / X,XXX,XXX회</div>          ← 총 노출수
<div class="kpi t">X,XXX,XXX / CTR X.XX%</div>       ← 총 조회수 (kpi t)
<div class="kpi t">XX원 / 3개 캠페인 운영 중</div>    ← 평균 CPV (kpi t)
<div class="kpi">XX,XXX / CPC XXX원</div>            ← 클릭수
```

---

### 3-5. 전환 탭 KPI

**HTML 위치**: `id="page-conv"` → `.kpi-grid` (5개 박스)

**데이터 소스**: `$wsConv` R184 (일별총합 6월합계)

```powershell
$cv_cost  = $wsConv.Cells(184,4)                                # 총 소진금액
$cv_imp   = $wsConv.Cells(184,5)                                # 총 노출
$cv_views = $wsConv.Cells(184,6)                                # 총 조회
$cv_click = $wsConv.Cells(184,7)                                # 총 클릭
$cv_ctr   = [Math]::Round($wsConv.Cells(184,9)*100, 2)          # CTR
$cv_conv  = [Math]::Round($wsConv.Cells(184,12))                # 전환건수
$cv_cvr   = [Math]::Round($wsConv.Cells(184,13)*100, 1)         # CVR(%)
$cv_cpsub = [Math]::Round($cv_cost / $cv_conv)                  # 구독단가
$cv_pct   = [Math]::Round($cv_cost / 100000000 * 100)           # 소진율(%)
$cv_costM = [Math]::Round($cv_cost / 10000)                     # 만원
```

**컬러 클래스 규칙**:
- 전환건수 → `kpi t` (teal)
- 구독단가 → `kpi a` (orange)
- 6월 광고비 → `kpi a` (orange)
- CTR → `kpi t` (teal)
- CVR → `kpi t` (teal)

---

## 4단계 — 개요 탭 camp-list 업데이트

**HTML 위치**: `id="page-overview"` → `.camp-list` → 4개 `.camp` 블록

각 캠페인 행에서 업데이트할 값:

| 캠페인 | 소진율 | 집행액 | 지표1 | 지표2 | 지표3 |
|--------|--------|--------|-------|-------|-------|
| 조회캠페인 | 조회집행액/3983% | 조회집행액만원 | CTR | CPV | 조회수 |
| 클릭최대화 | `$ck_pct`% | `$ck_costM`만원 | `$ck_ctr`% CTR | CPC | `$ck_click` 클릭 |
| 후속조회 | `$rt_pct`% | `$rt_costM`만원 | `$rt_ctr`% CTR | CPV | `$rt_views` 조회 |
| 전환 | `$cv_pct`% | `$cv_costM`만원 | `$cv_ctr`% CTR | `$cv_cpsub`원 구독단가 | `$cv_conv` 전환 |

**HTML 패턴** (각 camp 블록):
```html
<span style="color:var(--accent)">X% 소진 · Xman원</span>
<div class="pf" style="width:X%"></div>   ← progress bar width = 소진율%
<div class="cmv">X.XX%</div>             ← CTR
<div class="cmv">XX원</div>              ← CPC 또는 CPV
<div class="cmv">XX,XXX</div>            ← 클릭 또는 조회 또는 전환
```

---

## 5단계 — 클릭최대화 프로그램 카드 업데이트

**HTML 위치**: `id="page-click"` → `.prog-card` 3개 (라플위클리, HOT&NEW, 딥다이브)

**데이터 소스**: `$wsClick` R104 소재별 6월합계

```powershell
# 라플위클리 카드 (스필버그 C2~C11 + 토이스토리 C12~C21 합산)
$wk_cost  = $wsClick.Cells(104,4)  + $wsClick.Cells(104,14)
$wk_imp   = $wsClick.Cells(104,5)  + $wsClick.Cells(104,15)
$wk_views = $wsClick.Cells(104,6)  + $wsClick.Cells(104,16)
$wk_click = $wsClick.Cells(104,7)  + $wsClick.Cells(104,17)
$wk_ctr   = [Math]::Round($wk_click/$wk_imp*100, 2)
$wk_cpc   = [Math]::Round($wk_cost/$wk_click)

# HOT&NEW 카드 (C22~C31)
$hn_cost  = $wsClick.Cells(104,24)
$hn_imp   = $wsClick.Cells(104,25)
$hn_views = $wsClick.Cells(104,26)
$hn_click = $wsClick.Cells(104,27)
$hn_ctr   = [Math]::Round($wsClick.Cells(104,29)*100, 2)
$hn_cpc   = [Math]::Round($hn_cost/$hn_click)

# 딥다이브 카드: R104에서 열 위치 확인 필요 (C32~C41 추정)
# → R102 헤더 행에서 4번째 소재 그룹 열 확인 후 사용
```

각 카드의 업데이트 항목: 집행액, 노출, 조회, 클릭, CTR, CPC

**배지(badge) 규칙**:
- CTR 최고 소재: `class="badge bg"` (파란 배지)
- CPC 최저 소재: `class="badge bg"` (파란 배지)
- 그 외: `class="badge ba"` (회색 배지)

---

## 5-1단계 — 광고비/클릭수 도넛 차트 소재 데이터 업데이트

**HTML 위치**: `id="page-click"` → `<canvas id="clickDonutCost">`, `<canvas id="clickDonutClicks">`  
**JS 위치**: `drawDonut('clickDonutCost', [...])`, `drawDonut('clickDonutClicks', [...])`

**데이터 소스**: `$wsClick` 소재별 6월합계 행(R104) — 소재별 비용/클릭수

> ⚠️ **중앙 수치 하드코딩 금지** — `drawDonut(canvasId, slices, centerSub, fmt)`는 중앙에 표시되는 총계(예: `945만원`, `23.9만`)를 **slices 배열의 `v` 합계에서 자동 계산**한다 (`fmt` 콜백으로 단위만 지정).  
> 과거 이 값을 문자열로 직접 써넣다가(`'945만원'`처럼) 소재별 `v` 값은 갱신하고 중앙 텍스트는 깜빡 잊는 일이 반복돼서, 도넛 조각과 중앙 숫자가 서로 다른 값을 보여주는 버그가 여러 차례 재발했다 (커밋 `f6470f1`은 이 증상을 슬라이스 데이터 수정 없이 라벨 문자열만 바꿔 임시 봉합한 사례).  
> 지금 구조에서는 **소재별 `v` 값만 갱신하면 중앙 수치는 자동으로 맞다** — 별도로 손댈 부분 없음. 새 소재 추가/종료 시에도 슬라이스 배열만 수정하면 된다.

```js
drawDonut('clickDonutCost', [
  {lbl:'소재명 (프로그램)', v:비용, c:색상코드}, ...
], '6월 총 광고비', function(v){return Math.round(v/10000)+'만원';});

drawDonut('clickDonutClicks', [
  {lbl:'소재명 (프로그램)', v:클릭수, c:색상코드}, ...
], '6월 총 클릭수', function(v){return (v/10000).toFixed(1)+'만';});
```

---

## 6단계 — 후속조회 라플위클리 카드 업데이트

**HTML 위치**: `id="page-retarget"` → `.prog-card` (라플위클리 카드)

**데이터 소스**: `$wsRetgt` R55 소재별 6월합계 (토이스토리+실수+스필버그 합산)

```powershell
# 토이스토리(C2~C11) + 실수(C12~C21) + 스필버그(C22~C31) 합산
$rw_cost  = $wsRetgt.Cells(55,4)  + $wsRetgt.Cells(55,14) + $wsRetgt.Cells(55,24)
$rw_imp   = $wsRetgt.Cells(55,5)  + $wsRetgt.Cells(55,15) + $wsRetgt.Cells(55,25)
$rw_views = $wsRetgt.Cells(55,6)  + $wsRetgt.Cells(55,16) + $wsRetgt.Cells(55,26)
$rw_click = $wsRetgt.Cells(55,7)  + $wsRetgt.Cells(55,17) + $wsRetgt.Cells(55,27)
$rw_ctr   = [Math]::Round($rw_click/$rw_imp*100, 2)
$rw_cpv   = [Math]::Round($rw_cost/$rw_views)
```

업데이트 항목: 집행액, 노출, 조회, CTR, CPV

---

## 7단계 — 전환 퍼널 + 롱폼 callout 업데이트

**HTML 위치**: `id="page-conv"` → 퍼널 섹션 + callout 섹션

**데이터 소스**: `$wsConv` R184 (일별총합 6월합계) + R377 (소재별 6월합계)

### 전환 퍼널 (노출→조회→클릭→전환)

```powershell
# R184 기준
$cv_view_rate = [Math]::Round($cv_views/$cv_imp*100, 2)   # 조회율(%)
# 노출 C5, 조회율, 클릭 C7, CTR C9*100, 전환 C12, CVR C13*100, 구독단가
```

**HTML 패턴**:
```html
노출 X,XXX,XXX → 조회 XX,XXX(X.XX%) → 클릭 XX,XXX(CTR X.XX%) → 전환 X,XXX건(CVR XX.X%, 단가 X,XXX원)
```

### 롱폼 합계 callout

```powershell
# 소재별 6월합계 합산 (R377)
$cv_lf_cost = $wsConv.Cells(377,4) + $wsConv.Cells(377,14) + $wsConv.Cells(377,24)
```

업데이트 항목: 6월 광고비, 구독 전환건수, 구독단가, CTR, CVR

---

## 8단계 — 개요 탭 mbar CTR 업데이트

**HTML 위치**: `id="page-overview"` → "프로그램별 클릭 최대화 · CPC 비교" 카드 → `.mbar-row` 3개

**데이터 소스**: `$wsClick` R104 소재별 6월합계 CTR

```powershell
$mbar_weekly = [Math]::Round(($wsClick.Cells(104,7)+$wsClick.Cells(104,17)) /
               ($wsClick.Cells(104,5)+$wsClick.Cells(104,15)) * 100, 2)  # 라플위클리 CTR
$mbar_hot    = [Math]::Round($wsClick.Cells(104,29)*100, 2)               # HOT&NEW CTR
# 딥다이브 CTR: 딥다이브_조회 시트 6월합계 CTR 또는 클릭최대화 딥다이브 소재 CTR
```

**bar width 계산**: `라플위클리=100%` 기준으로 상대 비율 적용
```
HOT&NEW bar% = round(mbar_hot / mbar_weekly * 100)
딥다이브 bar% = round(mbar_deep / mbar_weekly * 100)
```

**HTML 패턴**:
```html
<div class="mbar-fill" style="width:100%"><span class="mbar-num">CTR X.XX%</span></div>   ← 라플위클리
<div class="mbar-fill" style="width:XX%"><span class="mbar-num">CTR X.XX%</span></div>    ← HOT&NEW
<div class="mbar-fill" style="width:XX%"><span class="mbar-num">CTR X.XX%</span></div>    ← 딥다이브
```

---

## 9단계 — JUNE 객체 업데이트 (클릭최대화 소재별 일별)

> 시작하기 전에 [체크리스트 A·B번](#-월-전환-시-필수-체크리스트-매월-1회-매우-중요)부터: 이미 아는
> 소재만 갱신하지 말고 시트의 소재별 데이터 섹션 헤더를 전부 스캔해 신규 소재 블록이 생겼는지
> 먼저 확인한다. 2026-07에 이걸 건너뛰어서 신규 소재 5개가 통째로 누락된 적이 있다.

### 시트: `디맨드젠_클릭최대화`

**소재별 섹션 위치**: R102 (헤더), R104 (6월합계), R105+ (일별)

**열 구조** (R102 헤더 행):
| 소재 | 날짜열 | 비용 | 노출 | 조회 | 클릭 | CPM | CTR | CPC | CPV |
|------|--------|------|------|------|------|-----|-----|-----|-----|
| 라플위클리_스필버그 | C2 | C4 | C5 | C6 | C7 | C8 | C9 | C10 | C11 |
| 라플위클리_토이스토리 | C12 | C14 | C15 | C16 | C17 | C18 | C19 | C20 | C21 |
| 핫앤뉴_16화_토이스토리 | C22 | C24 | C25 | C26 | C27 | C28 | C29 | C30 | C31 |

**HTML 입력 형식**: `[lbl, ep, prog, cost, imp, ctr, clicks, cpc, cpv]`

```javascript
{lbl:'토이스토리', ep:'위클리 S5 13화', prog:'weekly',
 cost: C14(반올림), imp: C15, ctr: C19*100(소수2자리), clicks: C17, cpc: C20(반올림), cpv: C11(반올림)}
{lbl:'스필버그',   ep:'위클리 S6 19화', prog:'weekly', ...}
{lbl:'토이스토리', ep:'HOT&NEW 16화',   prog:'hot',   ...}  ← cost > 0인 경우만 추가
```

**추가할 날짜**: HTML 마지막날 다음부터 `보고서기준일 - 1일`까지

---

## 10단계 — RD 객체 업데이트 (후속조회 일별 총합)

### 시트: `디맨드젠_후속조회`

**일별 총합 위치**: R17 (헤더), R19 (6월합계), R20+ (일별)

**열 구조**:
| C2(날짜시리얼) | C3(요일) | C4(비용) | C5(노출) | C6(조회수) | C7(클릭수) | C9(CTR) |

**HTML 입력 형식**:
```javascript
'6/N': {views: C6, ctr: (C9*100, 소수2자리), cpv: Math.round(C4/C6), cost: Math.round(C4)}
```

---

## 11단계 — RC 객체 업데이트 (후속조회 소재별)

### 시트: `디맨드젠_후속조회`

**소재별 섹션 위치**: R53 (헤더), R55 (6월합계), R56+ (일별)

**열 구조**:
| 소재 | 날짜열 | 비용 | 노출 | 조회 | 클릭 | CPM | CTR | CPC | CPV |
|------|--------|------|------|------|------|-----|-----|-----|-----|
| 라플위클리_토이스토리 | C2 | C4 | C5 | C6 | C7 | C8 | C9 | C10 | C11 |
| 라플위클리_실수(시즌1 9화) | C12 | C14 | C15 | C16 | C17 | C18 | C19 | C20 | C21 |
| 라플위클리_스필버그 | C22 | C24 | C25 | C26 | C27 | C28 | C29 | C30 | C31 |

**HTML 입력 형식**: `[조회수, 클릭수, CTR%(소수2자리), CPV(반올림), 비용(반올림)]`

**활성 소재 확인**: 해당 날짜 행의 C4(비용) > 0인 소재만 업데이트

**OFF 캠페인** (R87+): 원더풀스(핫앤뉴11화), 군체(딥다이브6화) — 종료일 이후 추가 불필요

---

## 12단계 — CONV 객체 업데이트 (전환 일별 총합)

### 시트: `디맨드젠_전환캠페인`

**6월 일별 총합 위치**: R184 (6월합계), R185+ (일별)

**열 구조**:
| C2(날짜시리얼) | C3(요일) | C4(비용) | C5(노출) | C6(조회수) | C7(클릭) | C9(CTR) | C12(전환건수) | C13(CVR) |

**HTML 입력 형식**:
```javascript
'6/N': {conv: C12(반올림), cvr: C13*100(소수1자리), cost: Math.round(C4)}
```

---

## 13단계 — CC 객체 업데이트 (전환 소재별)

### 시트: `디맨드젠_전환캠페인`

**소재별 섹션 위치**: R219 (헤더), R377 (6월합계), R225+ (1월1일~)

**열 구조** (R219 헤더):
| 소재 | 날짜열 | 비용 | 노출 | 조회 | 클릭 | CPM | CTR | CPC | CPV |
|------|--------|------|------|------|------|-----|-----|-----|-----|
| 딥다이브_6화_군체 | C2 | C4 | C5 | C6 | C7 | C8 | C9 | C10 | C11 |
| 시즌6_19화_스필버그 | C12 | C14 | C15 | C16 | C17 | C18 | C19 | C20 | C21 |
| 시즌5_13화_토이스토리 | C22 | C24 | C25 | C26 | C27 | C28 | C29 | C30 | C31 |

**6월 N일 행 위치**: `R375 + N - 1`  
(계산: R225 + 150(1월1일~5월31일) + (N-1))

**HTML 입력 형식**: `[조회수, 클릭수, CTR%(소수2자리), CPV(반올림), 비용(반올림)]`

**활성 소재만 추가**: 비용 > 0인 소재 (군체는 6/17 이후 종료, 스필버그/토이스토리는 계속 활성)

---

## 14단계 — CREATIVES 배열 업데이트 (조회캠페인 소재별)

> 이 배열은 **캠페인 기간 누적 합산**이므로 매일 전체 값을 갱신한다.
>
> ⚠️ **`update_dashboard.ps1`은 이 배열을 전혀 건드리지 않는다** — 여전히 100% 수동 작업.
> **실무 팁 (2026-07-07 확인)**: 조회캠페인 개별 소재는 예산이 정해진 "단발성 집행"이라 보통
> 며칠 안에 예산을 다 쓰고 스스로 종료된다(일별 행이 그냥 멈춰있음 — 삭제되는 게 아니라 그
> 날짜 이후로 행 자체가 없어짐). 즉 **어제 이미 갱신했던 기존 소재 대부분은 오늘도 값이 그대로일
> 가능성이 높다** — 매번 18~22화/13~17화/7~8화를 전부 다시 계산하려 하지 말고, 먼저 각 시트를
> `Find-CreativeBlocks` 스타일로 스캔해서 **"어제 목록에 없던 새 블록이 생겼는지"만 확인**하는
> 게 훨씬 빠르다(실제로 2026-07-06→07 하루 사이에 HOT&NEW 18화 "모아나" 하나만 신규였고,
> 나머지 20여 개 소재는 전부 이미 종료 상태라 손댈 게 없었음). 기존 소재의 "합계" 셀 값이
> CREATIVES에 이미 있는 값과 같으면 그 소재는 건드릴 필요 없다.

### 시트별 데이터 위치

**HOT&NEW_조회** (`HOT & NEW_조회`):
- 6월합계 행: R97 (total), 이후 회차별 R163+
- 핫앤뉴_N화: 각 회차 헤더 행 찾아 6월합계 읽기

**라플위클리_조회** (`라플위클리_조회`):
- 회차별 6월합계 읽기 (시트 구조 동일)

**딥다이브_조회** (`딥다이브_조회`):
- 회차별 6월합계 읽기

### HOT&NEW 16화 특별 처리

HOT&NEW 16화는 **조회캠페인 + 클릭최대화 캠페인** 합산값을 CREATIVES에 표시:
- `imp` = 조회시트 imp + `$wsClick.Cells(104,25)` (클릭최대화 6월합계)
- `views` = 조회시트 조회수 + `$wsClick.Cells(104,26)`
- `clicks` = 조회시트 클릭수 + `$wsClick.Cells(104,27)`
- `ctr` = total_clicks / total_imp * 100
- `vtr` = total_views / total_imp * 100
- `cpv` = total_cost / total_views (반올림)

### HTML 입력 형식

```javascript
{fmt:'롱폼', prog:'HOT&NEW', ep:'N화', title:'소재명',
 ctr: 소수2자리, vtr: 소수1자리, cpv: 정수, views: 정수, imp: 정수, clicks: 정수}
```

> **VTR 계산**: `vtr = views / imp * 100` (소수 1자리)

### 딥다이브 뉴뮤지엄 (7화) 소재 구성

딥다이브_조회 시트 R190 (6월합계 행)에서 **3개 소재** 읽기:

| fmt | ep | 비고 |
|-----|-----|------|
| 롱폼 | 7화 | 뉴뮤지엄 롱폼 |
| 쇼츠 | 7화 선공개 | 선공개쇼츠 |
| 쇼츠 | 7화 하이라이트 | 하이라이트쇼츠1 |

쇼츠는 2편(선공개 + 하이라이트쇼츠1)이다. 1편만 추가하지 않도록 주의.

### 롱폼/쇼츠 필터 버튼 개수 수동 관리

CREATIVES 배열에 항목 추가/삭제 시 테이블 상단 필터 버튼의 개수도 수동으로 함께 업데이트한다:

```html
<button onclick="filterFmt('롱폼')">롱폼 (N개)</button>
<button onclick="filterFmt('쇼츠')">쇼츠 (N개)</button>
```

버튼 숫자 = CREATIVES 배열에서 해당 fmt 항목 수 (코드에서 자동 계산되지 않음).

---

## 15단계 — 개요 탭 코멘트·인사이트·icard 업데이트

> 수치는 엑셀에서 계산, 텍스트는 당일 운영 현황 반영

### 개요 최신 코멘트 (HTML: `.sect-lbl` 다음 `.sum-row` 또는 comment 블록)

업데이트 항목:
- 날짜: `6/1~6/N` (N = 보고서기준일 - 1)
- 클릭최대화 클릭수·CTR: `$ck_click`만건, `$ck_ctr`%
- 후속조회 CPV·조회수: `$rt_cpv`원·누적 `$rt_views/10000`만 조회
- 전환 건수·단가: `$cv_conv`건·단가 `$cv_cpsub`원
- 활성 소재 현황: 당일 운영 중인 소재명 반영 (수동 확인)

### 월간 인사이트 섹션 (`.sum-insight`)

```html
<div class="sect-lbl">월간 주요 인사이트 — 6월 1일~N일 종합 분석</div>
```
- 날짜 텍스트 N 업데이트
- 전체 요약 문구에서 소진율·구독단가·소진금액 수치 업데이트

### icard 수치 업데이트 (`.icard-grid` 4개 카드)

| 카드 | 업데이트 수치 |
|------|-------------|
| 조회캠페인 | 예산 소진율% |
| 클릭최대화 | CTR%, 누적 클릭수(만건), 라플위클리 CTR%, HOT&NEW CTR% |
| 후속조회 | 누적 조회수(만 조회) |
| 전환 | 구독단가, CVR%, 누적 전환건수 |

### 월별 CTR 추이 차트 (`var months`)

클릭최대화 탭 내 "월별 CTR 추이" 차트는 `var months` 배열이 하드코딩되어 있다.  
월말 마감 시 `'6월(~28일)'` 형태의 텍스트를 실제 최종일로 직접 수정한다:

```javascript
var months = ['4월', '5월', '6월(~30일)'];  // 예: 6월 마감 반영 시
```

### 클릭최대화 프로그램별 클릭수 차트 (`clickProg`)

이 차트의 data 배열 형식은 **전환 롤링 차트와 다르다** — `c`(컬러) 속성이 필수:

```javascript
var data = [
  {lbl:'라플위클리', sub:'X,XXX만원', v: 클릭수, c: C.weekly},  // #4B8EFF
  {lbl:'HOT&NEW',   sub:'X,XXX만원', v: 클릭수, c: C.hot},     // #FF3B5C
  {lbl:'딥다이브',  sub:'X,XXX만원', v: 클릭수, c: C.deep}     // #22C55E
];
var maxV = 적절한_상한값;
```

`c` 속성이 없으면 `fillStyle=undefined`로 차트가 완전히 미표시된다. 항상 명시할 것.  
데이터 소스: 클릭최대화 탭 각 프로그램 카드의 클릭수 합계 / 집행액.

### retargetDaily raw 배열 (후속조회 일별)

HTML 내 `retargetDaily` raw 배열에 새 날짜 항목을 추가한다:

```javascript
{d:'6/N', views: RD['6/N'].views, ctr: RD['6/N'].ctr, cpv: RD['6/N'].cpv, cost: RD['6/N'].cost}
```

`ctr` 필드 = Excel CTR % 문자열 (예: `'3.49'`) — **× 100 하지 않음**, 그대로 문자열로 입력.  
단, `raw ctr` 계산 시: `parseInt(ctr * 100)` 방식으로 정수 변환하는 부분 확인 필요.

### 인사이트 카드 텍스트 (하드코딩 영역)

`.sum-insight` 섹션 내 서술형 문구는 **엑셀 수치로 자동 계산되지 않는다**. 수치 업데이트 후 수동으로 반영:

- 전체 소진율 문구: `"6,983만원 예산 중 XX% 소진"`
- 구독단가 문구: `"구독당 단가 X,XXX원 달성"`
- 누적 전환 문구: `"누적 X,XXX건 구독 전환"`
- 날짜 범위: `"6월 1일~N일 종합 분석"`

---

## 16단계 — 검증 체크리스트

업데이트 후 아래 항목을 자동 계산으로 검증한다.

```
[ ] 개요 KPI 총 광고비 = 조회+클릭+후속+전환 집행액 합산 ✓
[ ] JUNE '6/N' 비용합계 ≈ 디맨드젠_클릭최대화 일별총합 해당일 (R69+ C4)
[ ] RD '6/N' cost ≈ 후속조회 일별총합 해당일 R20+ C4 ±1원
[ ] CONV '6/N' cost ≈ 전환캠페인 일별총합 해당일 R185+ C4 ±1원
[ ] CC 군체+스필버그+토이스토리 합계 ≈ CONV cost ±100원
[ ] RC 토이스토리+실수+스필버그 합계 ≈ 후속조회_라플위클리 subtotal ±1원
[ ] CREATIVES HOT&NEW 16화 views = 조회시트 + 클릭최대화 합산 ✓
[ ] 클릭최대화 카드 집행액 합산 = KPI 총 집행액 ✓
[ ] 후속조회 카드 집행액 = R19 C4 만원단위 ✓
[ ] 전환 퍼널 전환건수 = R184 C12 ✓
[ ] 도넛 차트(clickDonutCost/clickDonutClicks) 중앙 수치는 자동 계산이므로 slices `v` 합계와 항상 일치 — 별도 검증 불필요, 하드코딩 회귀 여부만 확인 ✓
[ ] (월 전환 시) 엑셀 소재별 데이터 섹션 헤더 전수 스캔 결과 vs JUNE/CC/RD/CREATIVES에 있는 소재 이름 — 누락 0건 ✓
[ ] (월 전환 시) 프로그램별 합계(SUMMARY 표 등) = 소재별 데이터 합산 — 광고비/노출/조회/클릭 전부 일치 ✓
[ ] (월 전환 시) "일자별 OOO 현황" 3개 탭 모두 새 달 버튼 존재 + 클릭 시 정상 렌더 확인 (브라우저에서 직접 클릭)
[ ] 요일 표시가 있는 곳(`cv1`, 일자별 상세카드 노트 등)은 `day%7` 하드코딩이 아니라 `new Date(2026,mo-1,day).getDay()` 사용 확인
```

---

## 17단계 — GitHub 푸시 (GitHub Pages 자동 배포)

검증이 완료되면 아래 PowerShell 명령으로 변경사항을 GitHub에 푸시한다.  
푸시 즉시 `.github/workflows/deploy-pages.yml`(GitHub Actions)이 감지하여 **자동으로 배포**한다 (통상 30초~1분 소요).

```powershell
# 현재 작업 디렉토리 기준 (Set-Location 불필요)
git add "라플TV_캠페인_대시보드.html"
git commit -m "대시보드 업데이트: 6/N 데이터 반영"
git push origin main
```

> 커밋 메시지의 날짜(6/N)는 실제 업데이트한 날짜로 교체한다.  
> 푸시 후 GitHub Actions → GitHub Pages로 자동 배포한다 (통상 30초~1분).  
> 배포 URL: `https://sjh0927-hash.github.io/hanwha-ax-dashboard/라플TV_캠페인_대시보드.html`

---

## 검증 이력

### 실행 사례 — 260625.xlsx → 라플TV_캠페인_대시보드.html (6/24 반영)

| 단계 | 입력 | 출력 | 결과 |
|------|------|------|------|
| KPI 추출 | Grand total R16, 각 시트 6월합계 | 5개 탭 KPI 수치 | 개요·클릭·후속·전환 전체 갱신 |
| 일별 데이터 | 디맨드젠 3개 시트 일별 행 | JUNE·RD·RC·CONV·CC 객체 | 6/24 행 추가 완료 |
| 검증·배포 | HTML diff 확인 | git push → GitHub Pages | 30초 내 자동 배포 |

### 고친 흔적 (커밋 순)

1. **`4029032` 클릭최대화 예산 소진율 오류** — R104(소재별 6월합계)로 비용 합산했더니 383만원(38% 소진) 오산 → R68(일별총합 6월합계)이 실제 집행액 기준임을 확인하고 소스 행 수정 (945만원, 95%)
2. **`5cdfa0a` JS 구문 오류** — 미니바 IIFE 내부 `${...}` 닫기 괄호 누락으로 날짜버튼 파손 → 스킬에 "JUNE 객체 갱신 후 반드시 브라우저 날짜버튼 클릭 동작 확인" 체크 추가
3. **`94dd5ed` 딥다이브 CPV 미노출** — 조회캠페인 딥다이브 CPV 칸이 0으로 표시 → 딥다이브_조회 시트 6월합계 행 위치가 다른 IP와 다름을 확인; 14단계 CREATIVES 추출 전 헤더 행 탐색 단계 명시
4. **`replace_all 사이드이펙트` clickProg 차트 미표시** — 전환 롤링 7일 차트(`cv1`)와 프로그램별 클릭수 차트(`clickProg`)가 동일한 `var data=[{lbl:'6/XX',...}]` 패턴 공유 → replace_all로 교체 시 clickProg 데이터가 전환 데이터로 덮어써짐 → `d.c` 속성 없어 차트 미표시. clickProg 데이터는 `{lbl, sub, v, c}` 형식으로 수동 복원.
5. **`CC '6/29' 복사 버그`** — CC 객체에 새 날짜(6/29) 추가 시 군체/토이스토리/스필버그 3개 소재 모두 직전 날짜(6/26) 값이 그대로 복사됨. 실데이터 확인 후 수정. 신규 날짜 추가 후 반드시 직전 날짜 값과 비교 검증 필수.
6. **`뉴뮤지엄 쇼츠 1편 누락`** — CREATIVES 배열에 딥다이브 7화 뉴뮤지엄 쇼츠가 1편(선공개)만 있고 하이라이트쇼츠1 누락. 딥다이브_조회 R190 기준 3개 소재(롱폼+선공개쇼츠+하이라이트쇼츠1) 모두 추가. 롱폼/쇼츠 버튼 개수도 함께 수정.
7. **`Grand total 열 매핑 오류`** — 스킬 문서에 C9=노출, C10=조회, C11=클릭으로 오기재 → 실제는 C6=노출, C7=조회, C8=클릭, C12=소진금액. 스킬 문서 수정 완료.
8. **도넛 차트 중앙 수치 미갱신 반복 재발** — `drawDonut()` 중앙 총계(`945만원`, `23.9만`)가 문자열로 하드코딩돼 있어 소재별 `v` 값을 갱신해도 중앙 텍스트는 안 바뀌는 일이 반복됨 (`f6470f1`은 슬라이스 데이터는 그대로 두고 라벨 문자열만 손으로 고쳐 임시 봉합 — 근본 원인 미해결). **구조적으로 수정**: `drawDonut(canvasId, slices, centerSub, fmt)`가 `slices` 합계를 `fmt` 콜백으로 포맷해 자동 표시하도록 변경 → 중앙 수치를 손으로 만질 필요 자체를 제거. 상세: [5-1단계](#5-1단계--광고비클릭수-도넛-차트-소재-데이터-업데이트).
9. **(2026-07-06) 1일 오프셋 규칙 변경** — 사용자 확인: 데이터 제공처가 엑셀 파일명 날짜의 의미를 "생성일(익일)"에서 "마감일자(당일)"로 변경. `update_dashboard.ps1` 47번 줄 `$endDay = [int]$fnDate.Substring(4,2) - 1`에서 `- 1` 제거. 새로 받은 `..._260705.xlsx`부터 적용. **주의**: 위 "핵심 개념" 섹션도 함께 갱신했으니, 과거 파일을 다시 다룰 일이 있으면 그 파일이 신규칙/구규칙 중 어느 쪽인지 날짜로 먼저 판단할 것.
10. **(해결, 2026-07-06) `update_dashboard.ps1` 월 하드코딩 → 월 전환(Month Rollover) 로직 추가** — "6월"이 리터럴로 박힌 곳이 실제로는 40곳 넘게 있었음(섹션 제목·차트 제목·테이블 헤더·KPI 라벨·도넛 중앙 라벨·JS 날짜 문자열까지). 이걸 3종류로 분류해서 처리:
   - **구조적 라벨(자동화 완료)**: `<title>`, `hdr-sub`, `sect-lbl`, `ct`(차트제목), `th`(테이블헤더), `kl`(KPI라벨), 도넛 중앙 라벨(`6월 총 광고비`/`6월 총 클릭수`), 일자별 상세보기 JS 문자열(`6월 최고/최저 전환일·조회일`, `noteEl.innerHTML='6월 '+day+...`) 등 26개 이상의 정확한 old→new 텍스트 쌍을 `[ordered]@{}` 해시테이블로 등록하고, HTML `<title>`의 월 숫자와 이번 파일의 `$month`가 다를 때만 일괄 치환하는 "5-A2. 월 전환" 블록을 새로 추가. 5-B(기준일)·5-C(KPI 라벨)·5-E(날짜 텍스트) 안의 하드코딩된 "06"/"6월"/"6/1~"도 전부 `$month` 기반으로 일반화.
   - **날짜범위 "(N/1~N/day)"(자동화 완료, 5-E2)**: 차트제목·섹션라벨 `class="ct"`/`"sect-lbl"` 안에 있는 `(6/1~6/4)` 형태만 정규식으로 스코프를 잡아 매주 자동 갱신. `op-comment`(사람이 쓰는 요약 코멘트) 블록은 이 스코프 밖이라 자동으로 보호됨.
   - **자동화 제외(의도적, 사람이 직접 처리)**: `op-comment`의 서술형 코멘트(예: "6월 마감", "CTR 추이: 4월→5월→6월"), 소재별 운영기간(예: "6/9 ~ 현재" — 실제 소재 시작일), 캠페인 시작일(예: "리타겟팅 · 6월 1일 개시"), 3개월 트렌드 배열 `months=['4월','5월','6월(~30일)']`(항목 9 참고, 데이터도 같이 바꿔야 해서 스크립트가 손댈 수 없음), 소재별 인라인 합계(예: "6월 광고비 8,614,871원" — 라벨만 바꾸면 숫자가 안 맞는 거짓 정보가 됨), CSS/JS 주석(화면에 안 보임).
   - **테스트 방법**: Excel COM 의존성 때문에 스크립트 전체를 실행해볼 수 없어서, `update_dashboard.ps1`에서 5-A2~5-E2 블록만 추출해 실제 HTML에 대고 `$month=7,$endDay=5`로 dry-run 후 "6월" 잔여 개수를 스캔해 각각이 의도한 카테고리인지 전수 확인하는 방식으로 검증(56개 → 18개, 남은 18개 전부 의도적 보존 대상 확인).
   - **버그 2건 발견·수정**: (1) PowerShell에서 `@(@(a,b),@(a,b))` 배열의 배열은 자동 평탄화돼서 `$pair[0]`이 "문자열 전체"가 아니라 "첫 글자 한 개"가 되는 버그 — old 텍스트가 전부 유일하므로 해시테이블로 대체해 회피. (2) `[regex]::Replace`에서 치환 문자열에 `` `$1``$2 `` 백틱이스케이프 템플릿을 쓰면 PowerShell 큰따옴표 처리와 얽혀 리터럴 "$1"이 그대로 출력되거나 훨씬 앞쪽 콘텐츠까지 삭제되는 버그 — `[System.Text.RegularExpressions.MatchEvaluator]` 스크립트블록으로 대체해 원천 차단.
   - **테스트 스크립트 자체의 인코딩 함정**: Node.js로 `.ps1` 조각을 추출해 BOM 없이 저장하면 Windows PowerShell 5.1이 한글을 mojibake로 읽어 문법 오류가 남 → 반드시 `[System.Text.Encoding]::UTF8`로 읽고 `New-Object System.Text.UTF8Encoding $true`(BOM 포함)로 다시 저장한 뒤 실행할 것(`실행.bat`이 `extract_lifeplus_data.ps1`에 이미 적용 중인 것과 동일한 패턴).
11. **(2026-07-07) 클릭최대화 탭 신규 소재 5개 누락 + 프로그램 오배정 발견·수정** — 개요 페이지 요일 오류를 계기로 클릭최대화 탭 전체를 재검토하다가, JUNE 객체가 뮤지컬1(20화)·뮤지컬2(21화)·더베어(HOT17화)·뉴뮤지엄(딥7화)·장송의프리렌(딥8화) 5개 신규 소재를 통째로 누락하고 있고, 스필버그·토이스토리(13화) 7월 값도 6월 말 값이 복사돼 부풀려져 있던 걸 발견. 디맨드젠_클릭최대화 시트 "1. SUMMARY"·"2. 소재별 데이터" 섹션을 직접 재추출해서 KPI·프로그램별 성과·소재별 테이블·CTR/클릭수 비교·도넛 2개·clickProg 차트까지 전부 재계산. 처음 clickProg를 고칠 때는 이 발견 전이라 딥다이브 몫을 HOT&NEW로 잘못 배정한 채 배포했다가 재수정한 이력 있음 — 새 소재 탐지(체크리스트 A번) 없이 프로그램별 합계부터 계산하면 이런 오배정이 재발한다. 상세: [월 전환 체크리스트](#-월-전환-시-필수-체크리스트-매월-1회-매우-중요) A·B·F번.
12. **(2026-07-07) `day % 7` 요일 계산 하드코딩이 월 경계마다 깨짐** — 개요 `cv1` 차트, 클릭최대화/전환/후속조회 3개 탭의 "일자별 OOO 현황" 섹션에서 전부 발견. `new Date(2026, month-1, day).getDay()`로 교체. 상세: 체크리스트 C번.
13. **(2026-07-07) 전환/후속조회 "일자별 현황" 렌더 함수에 `mo` 미정의 런타임 에러 위험** — 노트 텍스트의 "7월 N일" 하드코딩을 `replace_all`로 한 번에 고치면서, 아직 `mo` 변수를 파싱하지 않는 함수(후속조회)에도 `mo` 참조가 섞여 들어감. `replace_all`을 여러 함수에 걸쳐 쓸 때는 그 즉시 각 함수의 변수 정의도 같이 맞춰야 한다.
14. **(2026-07-07) 후속조회 캠페인 "미집행" 표기 컨벤션 확립** — 프로그램별 성과 카드·소재별 테이블·일별 추이 차트가 전부 6월 값을 그대로 보여주고 있어서, 미집행 캠페인 표기 규칙(배지·opacity·0/`-`·상태값 변경)을 이번에 처음 정립. 상세: 체크리스트 G번.
15. **(2026-07-07) 예산이 매월 재편성된다는 사실 확인** — 조회 3,983만→3,100만, 클릭최대화 1,000만→1,500만, 후속조회 1,000만→500만(미집행), 전환 1,000만→2,500만으로 전부 바뀜. 소진율%를 구예산으로 계산하면 틀리므로, 이 스킬 문서의 "예산 고정값" 표도 매월 갱신 대상으로 전환.
16. **(2026-07-07) `update_dashboard.ps1` 자체도 11번과 같은 컬럼 오배정 버그를 갖고 있던 것 확인·수정** — `$JUNE_LIST`가 고정 컬럼(c=4/14/24, `$clickMonthRow` 기준)으로 소재를 읽고 있었는데, 이 행이 사실 "2.Daily" 프로그램별 합계 섹션이라 스필버그=전체합계, 토이스토리(13화)=라플위클리 프로그램 합계, 토이스토리(HOT16화)=딥다이브 프로그램 합계를 읽고 있었음(11번에서 HTML에 발견한 것과 동일한 근본 원인, 스크립트 쪽에도 그대로 있었음). KPI의 노출/조회/클릭(`$ki/$kv/$kk`)도 같은 행을 컬럼별로 합산하면서 전체+라플위클리+딥다이브를 이중 계산(핫앤뉴는 누락)하는 별개 버그가 있었음. `Find-CreativeBlocks`(소재명으로 헤더 직접 탐색)+`Find-ClickSummaryRow`("1. SUMMARY" 표를 라벨로 탐색)로 전면 교체, `$CLICK_CREATIVES` 목록에 신규 소재 5개도 추가했고 "엑셀엔 있는데 목록엔 없는 블록"을 자동 경고하는 로직도 추가함. `cvDow[$d%7]`(12번과 동일한 요일 버그)도 `Get-Date`로 교체. 새 로직은 260705.xlsx로 실제 실행해서 SUMMARY 표·소재별 월누계·일별 값을 전부 사람이 검증한 숫자와 소수점 단위까지 대조 완료. **주의**: 이 스크립트는 `.gitignore` 대상이라 git 이력에 안 남으므로, 다음에 이 스크립트를 다시 열어볼 사람은 이 항목을 참고할 것.
17. **(2026-07-07) 문서 맨 위 "replace_all 절대 금지" 경고가 스크립트에서 실제로 재발** — 16번 수정 직후 바로 다음 날 데이터(`260706.xlsx`)로 스크립트를 처음 실행했더니, 문서 최상단 경고에 정확히 나온 그 사고가 실제로 두 군데서 터짐:
    - **cv1 ↔ clickProg**: `$html -replace '(var data=)\[[^\]]+\](;\s*var pad=\{t:18)',...` — PowerShell `-replace`는 기본이 전역 치환이라, cv1과 clickProg가 똑같이 갖고 있는 이 패턴이 둘 다 바뀌어버림. clickProg가 cv1의 구독전환수 데이터로 통째로 덮어써짐(`d.c` 없어서 차트 미표시까지는 안 갔지만 완전히 엉뚱한 값 표시).
    - **CD ↔ retargetDaily**: `[regex]::Matches($html,"\{d:'\d+/\d+',")` 로 "$html 전체"에서 마지막 매치를 찾는 방식 — `CD`(전환 일별) 배열과 `retargetDaily`의 `raw` 배열이 똑같은 `{d:'N/N',...}` 모양이라, 파일 뒤쪽에 있는 `retargetDaily` 쪽이 "마지막 매치"로 걸려서 CD에 들어가야 할 값이 엉뚱하게 거기 꽂히고 CD 자신은 갱신 안 됨(후속조회는 미집행인데 전환 데이터가 섞여 들어감).
    - **왜 16번 수정 때 안 잡혔나**: 16번은 클릭최대화 소재 데이터(JUNE/KPI)만 검증했고, cv1/clickProg·CD/retargetDaily는 손을 안 댄 기존 코드라 "안 건드렸으니 괜찮겠지"라고 넘어갔다. **교훈**: 스크립트 일부를 고칠 때, 그 스크립트 안에 문서 경고에 나온 것과 같은 유형의 패턴(전역 치환, `$html` 전체 대상 탐색)이 다른 곳에도 있는지 훑어보고 같이 스코프를 좁혀야 한다 — "내가 안 건드린 코드는 안전하다"는 가정이 틀렸다.
    - **수정**: 두 군데 다 JUNE 삽입 로직과 같은 패턴(먼저 `var CD=[`/`setup('cv1'` 같은 고유 앵커를 찾고, 그 앵커 이후의 **좁은 범위 안에서만** 검색·치환)으로 바꿈. `cv1`은 앵커 뒤 400자 윈도우 + `[regex]::Replace(...,count:1)`으로 1건만 치환하도록 이중 안전장치 적용.
    - **HTML 쪽도 긴급 수정**: 이미 잘못 배포된 260706 반영본에서 clickProg 데이터 복구, CD에 7/6 추가, retargetDaily 7/6을 0으로 정정, 자동화가 안 건드리는 "소재별 성과" 신규 소재 5개 행도 엑셀에서 재추출해 7/1~7/6 누적으로 수동 갱신, 일자별 현황 3탭 날짜버튼 `<=5`→`<=6`도 수동 반영.
    - **남은 과제**: `CONV` 삽입(`'\d+/\d+':\{conv:` 패턴)은 지금은 유일한 패턴이라 안전하지만, 앞으로 비슷한 모양의 배열이 새로 생기면 똑같이 터질 수 있다. 새 배열/차트를 추가할 때마다 기존 패턴과 모양이 겹치지 않는지 확인하는 걸 습관화할 것.
18. **(2026-07-07, 같은 날 해결) `$CC_MAP`(전환 소재별)도 16번과 같은 고정-컬럼-오프셋 문제로 신규 소재 데이터가 다른 소재 키에 덮어써짐** — 전환 캠페인에 신규 소재 딥다이브 8화 "장송의프리렌"이 투입되면서, 스크립트가 `$CC_MAP`의 고정 컬럼(`c=24` 등, `$convCreativeMonthRow` 기준)으로 '토이스토리' 데이터를 읽었는데 실제로는 그 컬럼이 신규 소재 쪽으로 밀려 있어서 **토이스토리 키에 장송의프리렌의 값이 그대로 들어감**(군체·스필버그는 우연히 컬럼이 안 밀려서 정상). 처음엔 HTML만 응급 수정하고 스크립트는 미뤘는데, 같은 날 안에 `$CC_MAP`/`$RC_MAP`을 `$CC_CREATIVES`/`$RC_CREATIVES`+`Find-CreativeBlocks`/`Match-CreativeBlock`/`Get-CreatureMonthTotal` 기반으로 전면 교체 완료(16번의 `$CLICK_CREATIVES`와 동일 패턴). `$rw_c`(후속조회 라플위클리 카드)·`$ccCreative`(개요 "소재별 CPV" mbar)도 마커행 직접 참조 대신 블록별 1일~당일 직접 합산으로 교체. 개요 mbar는 HTML에 슬롯이 3개뿐이라 소재 4종 중 3종(군체·스필버그·토이스토리)만 고정 표시하도록 범위를 명확히 함(장송의프리렌은 "소재별 성과" 테이블·일자별 카드에서 확인).
    - **추가로 발견한 버그**: `Match-CreativeBlock`이 이름을 단순 부분일치로 찾는데, 엑셀 헤더가 "장송의 프리렌"(공백 있음)인데 소재 목록엔 "장송의프리렌"(공백 없음)으로 등록해서 처음엔 매칭 자체가 실패했다(블록을 아예 못 찾음). `Match-CreativeBlock` 내부에서 검색어·후보명 양쪽 다 공백을 제거하고 비교하도록 수정 — 소재명에 공백이 있는지 없는지는 엑셀 작성자마다 다를 수 있으니 항상 공백-무관 비교를 기본으로 할 것.
    - **검증**: 260706.xlsx로 새 로직만 따로 뽑아 실행해서 군체·스필버그·토이스토리·장송의프리렌 4종의 블록 위치·1~6일 누계·7/6 일별값을 전부 확인, HTML에 이미 반영된 값과 소수점 단위까지 일치 확인.
19. **(2026-07-07) "조회캠페인 전체(디맨드젠 포함) 소진율 양호" — 애매한 서술형 문구가 실제 KPI 수치와 별개 개념을 암시하던 것 정리** — 개요 탭 "월간 주요 인사이트" 섹션의 icard 문구 중, 원래 있던 구체적 수치(예전엔 18.7% 같은 숫자였음)가 며칠 전 "소진율 양호"로 뭉뚱그려진 채 방치돼 있었다. 재계산하려다 보니 애초에 "조회캠페인 탭 KPI(741만원/3,100만·24%, 위클리+HOT&NEW+딥다이브 3개 캠페인 합산)"와 "조회캠페인 전체(디맨드젠 포함)"이 서로 다른 두 숫자인 것처럼 문구가 쓰여 있었는데, 사용자 확인 결과 조회캠페인 탭 비용은 애초에 디맨드젠을 섞지 않고 이 3개 캠페인 합산만 쓰는 게 맞다는 게 재확인됨. **수정**: 디맨드젠을 섞어 말하는 두 번째 문구를 통째로 없애고, 이미 정확했던 24% 수치에 "위클리·HOT&NEW·딥다이브 조회캠페인 합산" 근거를 명시하는 문구 하나로 통합(중복 제거). [3-2단계](#3-2-조회캠페인-탭-kpi)에도 같은 취지의 경고 박스 추가 — 앞으로 서술형 코멘트를 쓸 때 조회캠페인과 디맨드젠 소진율을 절대 한 문장에 섞지 말 것.

**2026-07-07 마무리**: 이날 발견된 이슈(신규 소재 누락 5건, 요일 계산, `mo` 미정의, 미집행 표기, 예산 재편성, 스크립트 컬럼 오배정 3건, replace_all 충돌 재발 2건, 소진율 문구 혼선) 전부 해결 완료. `update_dashboard.ps1`은 클릭최대화·전환·후속조회 3개 시트 전부 이름 기반 동적 블록 탐색(`Find-CreativeBlocks`/`Match-CreativeBlock`/`Find-BlockDayRow`/`Get-CreatureMonthTotal`)으로 통일돼 있으므로, 다음 신규 소재가 추가돼도 이 문서의 [월 전환 체크리스트](#-월-전환-시-필수-체크리스트-매월-1회-매우-중요) A~G번만 순서대로 따라가면 같은 유형의 버그는 재발하지 않을 것으로 기대.

20. **(2026-07-08) 260707.xlsx 최초 실행에서 스크립트 버그 3건 + 데이터 누락 1건 신규 발견·수정** — 07-07 대개편 이후 처음으로 실제 실행해보니 곧바로 새 문제가 터짐:
    - **PowerShell 해시테이블 열거 중 수정 오류** — `$RC_CREATIVES` 합산부(`foreach($key in $rwSum.Keys){$rwSum[$key]+=$t[$key]}`)가 `.Keys`를 열거하면서 동시에 값을 수정해 "Collection was modified" 예외로 즉시 중단. `foreach($key in @($rwSum.Keys))`로 키 목록을 먼저 배열로 복사해 해결(값 수정이 열거 자체를 무효화하는 .NET Hashtable의 특성).
    - **캠페인 카드(`.camp`, 개요 탭 4개) — "어제 결과값"을 하드코딩한 old-line 매칭이 하루 만에 깨짐** — `Set-CampLine`이 리터럴 old 문자열(예: "예산 3,983만원...83% 소진")과 정확히 일치해야 치환하는 방식이었는데, 이미 전날 실행으로 그 old 값 자체가 사라진 상태라 4개 카드 전부 "[기존 라인을 못 찾음]"만 찍고 하루 뒤처진 채 방치될 뻔했다. `Set-ClickRow`처럼 "숫자는 와일드카드, 구조만 고정" 방식의 `Set-CampBlock`(정규식 기반, cnm 앵커로 `.camp` 블록 통째로 잘라내 그 안에서 각 필드를 재교체)으로 전면 교체 — 이제 실행 시점 값이 무엇이든 항상 갱신됨.
    - **"프로그램별 성과" 카드(`.pcard`, 라플위클리/HOT&NEW/딥다이브)를 스크립트가 아예 건드리지 않고 있었음** — 원래 스킬 문서 5단계의 수동 작업으로만 존재했는데, 신규 소재 5~6개가 투입된 이후로도 옛날 값(스필버그+토이스토리13화만 반영)에 멈춰 있어서 개요 KPI 총 집행액(335만원)과 3개 카드 집행액 합(239만원)이 어긋나 있었다. `Set-PcardBlock` 추가로 자동화. **구현 중 추가로 두 가지 함정을 만남**: (1) 집계 계산을 5단계(HTML 업데이트, `$wb.Close()` 이후)에 넣었다가 죽은 Excel COM 객체를 참조해 `0x800A01A8` 예외 — 반드시 4단계(KPI 집계, Close 이전)로 옮겨야 함. (2) `$CLICK_CREATIVES` 소재를 prog별로 묶어 직접 합산했더니 광고비·노출은 시트의 "1. SUMMARY" 표 프로그램별 행(R10~R12)과 소수점까지 일치했는데 **클릭수만 6천 이상 차이** — 원인 규명 대신 SUMMARY 표를 그대로 읽는 쪽으로 교체(더 간단하고 근본적으로 안전).
    - **신규 소재 탐지 로직의 row-only 비교 버그로 "라플위클리 시즌6_22화_시선"(클릭최대화, 7/6 신규 투입)이 통째로 누락** — `$knownRows`가 row 번호만으로 "이미 아는 소재"를 판단했는데, 한 엑셀 행에 소재 여러 개가 컬럼만 다르게 나란히 배치된 경우(예: row=200에 뉴뮤지엄/장송의프리렌/더베어/시선 4개가 각각 다른 컬럼) 그중 하나라도 등록돼 있으면 같은 행의 나머지는 전부 "이미 아는 소재"로 오판돼 경고조차 안 떴다. 사용자가 "핫앤뉴 3편 업데이트"라 말했던 것과 실제 신규 확정 편수가 안 맞아 다른 스킬([generate-tv-score](generate-tv-score.md))에서 교차검증하던 중 우연히 발견 — 시선의 7/1~7/7 누적 광고비(49,850원)가 정확히 프로그램카드-KPI 총액 간 차액과 일치해 확정. **수정**: (1) `$CLICK_CREATIVES`에 시선 추가, (2) 클릭최대화·전환·후속조회 3개 시트의 신규소재 감지 로직 전부 `"$row:$col"` 쌍 비교로 교체(같은 버그가 CC_CREATIVES/RC_CREATIVES 쪽에도 잠재해 있었음, 재검사 결과 현재는 실제 누락 없음 확인). "소재별 성과" 수동 테이블에도 시선 행 신규 추가.
    - **"일자별 OOO 현황" 날짜 버튼이 3개 탭 모두 어제(7/6)에 멈춰 있었음** — 5-D 단계가 `for(var d2=1;d2<=N;d2++)` 패턴만 갱신하는데, 07-06 월 전환 이후 실제 HTML의 "이번 달" 루프 변수명은 `d3`로 바뀌어 있어서(이전 달=d2 고정 1~30, 이번 달=d3 가변) 이 치환이 조용히 아무것도 안 바꾸고 있었다. `d3` 대상 치환(루프 상한 + `' on'` 선택 클래스, `' ev'` 피크일 마커는 건드리지 않음)을 추가하고, 이미 어긋나 있던 HTML은 수동으로 7→7 반영.
    - **교훈**: (1) 어제자 값을 리터럴로 박아두는 "old→new 텍스트 치환" 패턴은 하루만 지나도 깨지는 시한부 코드다 — 처음부터 Set-ClickRow류의 와일드카드 매칭으로 짤 것. (2) "row 번호만으로 소재 식별"은 한 행에 여러 소재가 배치되는 이 시트 구조와 근본적으로 안 맞는다 — 소재 식별은 항상 (row,col) 쌍으로. (3) 사용자가 말한 "N편 업데이트" 같은 진술은 다른 스킬 실행 중 교차검증 계기가 되기도 하니, 서로 다른 스킬 간 수치가 안 맞으면 그 자체가 버그 신호일 수 있다.

21. **(2026-07-08) 조회캠페인 차트 통일 + 오버플로 방지 + clickProg 완전 하드코딩 해소** — 사용자 요청으로 3건 추가 개선:
    - **조회캠페인 하단 4개 차트 형태 통일** — VTR·CPV·CTR은 `drawHBar`(소재 수만큼 세로로 늘어나는 가로 막대 리스트, 카드 높이가 데이터량에 맞춰 자동으로 커짐)인데 조회수만 `drawVBar`(고정 높이 160px, 소재가 늘수록 막대가 점점 얇아짐)를 써서 2x2 그리드에서 같은 줄(CTR·조회수)의 카드 높이가 안 맞았다. 조회수도 `drawHBar`로 통일(maxV는 데이터 최댓값 기준 동적 계산) — 이제 네 카드 모두 같은 렌더러를 쓰므로 높이가 항상 맞는다.
    - **`drawHBar` 값/라벨 텍스트가 카드 밖으로 넘칠 수 있던 문제** — 원래 `lpad=100,rpad=50` 고정값이라, 조회수처럼 값이 6자리(예: `338402회`)로 커지거나 소재명이 길어지면 우측 라벨이 캔버스 밖으로 잘려 나갈 수 있었다. `ctx.measureText()`로 그 차트의 실제 라벨·값 텍스트 폭을 미리 측정해서 `lpad`/`rpad`를 동적으로 계산하도록 변경 — 최댓값 막대 + 최장 텍스트 조합에서도 항상 10px 이상 여백이 남도록 수식으로 보장(계산 근거는 코드 주석 참고). 동시에 값 표시를 `d.v+unit` → `d.v.toLocaleString()+unit`으로 바꿔 천단위 쉼표도 적용(VTR·CPV·CTR·조회수 4개 차트 전부 공통 함수라 한 번에 적용됨).
    - **"프로그램별 클릭수 / 집행액 비교"(`clickProg`) 차트가 완전 하드코딩이라 데일리 갱신이 안 되고 있었음** — `var data=[...]`가 특정 날짜의 스냅샷 값(72만원/172만원/43만원 등)으로 박혀 있었고, `maxV=45000`도 고정이라 클릭수가 그 이상으로 늘면 막대가 상단을 뚫고 나갈 수 있는 구조였다. (1) HTML 쪽: `maxV`와 눈금(0.25/0.5/0.75/1 지점)을 `data`의 실제 최댓값 기준으로 매번 재계산하도록 JS 로직 변경(10% 이상 여유를 두고 5,000 단위로 올림) — 이러면 앞으로 PowerShell이 `data` 값만 갱신해도 축이 알아서 맞는다. (2) `update_dashboard.ps1` 쪽: 4단계에서 이미 SUMMARY 표 기준으로 정확히 계산해 둔 `$progSums`(20번 항목의 클릭수 불일치 버그 수정 때 만든 것)를 그대로 재사용해 `clickProg`의 `data` 배열을 매일 갱신하는 5-J 단계 추가. **주의**: 이 차트도 `var data=[...]; var pad={t:18` 패턴이 `cv1`(구독전환 롤링 차트)과 완전히 동일해서, 문서 최상단 경고 및 17번 항목의 사고와 같은 유형의 충돌 위험이 있었다 — `cv1` 블록과 동일하게 `setup('clickProg'...)` 앵커 뒤 400자 윈도우로 범위를 좁히고 그 안에서만 정규식 치환하는 방식을 그대로 재사용해서 피함.

22. **(2026-07-08) 클릭최대화 소재별 차트 4종(`clickCTR`/`clickCreativeClicks`/`clickDonutCost`/`clickDonutClicks`) 자동화 + 엑셀 자체의 실제 데이터 버그 발견** — 사용자가 "이 4개도 데일리 자동화 필수"라고 요청.
    - **자동화**: `$CLICK_CREATIVES`에 `shortEp`(짧은 회차 라벨, 예: `20화`/`딥8화`)·`dotColor`(프로그램 내 소재 구분용 고정 색상) 필드 추가. 4단계(Excel 닫기 전)에서 전체 소재의 1~$endDay 누적(비용/노출/조회/클릭/CTR)과 "$endDay 당일 비용>0" 기준 활성여부를 `$matStats`로 계산하고, 5단계에서 `Set-JsArray`(앵커 뒤 첫 "];" 또는 특정 종료 문자열까지를 배열 내용으로 교체 — 개행 포함 앵커 대신 이걸 쓴 이유는 CRLF 파일에서 PowerShell의 `` `n ``(LF)이 실제 `` `r`n ``과 정확히 일치하지 않아 못 찾는 사고를 피하기 위함) 도우미로 `CDATA`/`CCLICKS`/두 도넛 slices를 전부 갱신.
    - **엑셀 데이터 버그 발견(중요, 사람이 고쳐야 함)**: 신규 소재 "시선"(라플위클리 22화)의 클릭수가 "더베어"(핫앤뉴 17화)와 날짜별로 완전히 동일한 값을 보여서 발견. 두 소재의 "클릭" 헬퍼컬럼 수식을 직접 열어보니 시선의 클릭 수식이 `=SUMIFS(RAW!$G:$G,...,RAW!$D:$D,$V$200)`으로, `$V$200`(더베어의 소재명 셀)을 그대로 참조하고 있었다 — 시선의 비용/노출/조회 수식은 전부 자기 이름칸(`$AF$200`)을 올바로 참조하는데 클릭 수식 하나만 옆 소재 컬럼을 복사하면서 절대참조를 안 고친 것. RAW 시트를 소재명으로 직접 재집계해서 시선의 실제 클릭이 1,033건(헬퍼컬럼이 보여주던 7,001건이 아님, CTR도 13.36%가 아니라 1.97%)임을 확인. **엑셀 원본 수정이 필요함 — 담당자에게 전달할 것: `디맨드젠_클릭최대화` 시트에서 "시선" 소재 블록의 클릭수 헬퍼컬럼(AK열, 203~209행 등) 수식의 `$V$200` 부분을 `$AF$200`으로 고쳐야 함.**
    - **스크립트 쪽 방어 로직 추가**: 소재별 클릭수를 헬퍼컬럼 값만 믿지 않고 RAW 시트에서 소재명(정확히 일치) + 캠페인 + 날짜 조건으로 직접 재계산(`Get-RawClicks`)해서 대조 — 1 이상 차이 나면 경고를 띄우고 RAW 값으로 자동 교정한다. 이번 실행에서 시선을 제외한 8개 소재는 전부 헬퍼컬럼과 RAW 값이 일치해 이 버그가 시선 1건에만 국한됨을 확인. **주의**: RAW 시트의 날짜 컬럼이 COM `Value2`로 `[datetime]` 객체로 오는 경우와 숫자 시리얼로 오는 경우가 섞일 수 있어서, 최초 구현 때 `-isnot [datetime]`으로만 걸러 전부 스킵되는 버그가 있었다(모든 소재가 RAW=0으로 나옴) — 두 타입 다 처리하도록 수정.
    - **파급 범위 확인**: `$progSums`(프로그램카드·clickProg 차트용, 20번 항목에서 SUMMARY 표 기준으로 전환)는 SUMMARY 표 자체가 RAW를 직접 SUMIFS하는 구조라 이 버그의 영향을 받지 않음(확인 완료, 재작업 불필요). 영향은 소재 단위 세부 지표(이번에 자동화한 4개 차트, 그리고 전날 수기로 추가한 "소재별 성과" 표의 시선 행)에만 국한됐고, 둘 다 1,033클릭/1.97%/48원으로 수정 완료.

23. **(2026-07-08) 전환캠페인 탭 "소재별" 테이블 — 토이스토리 종료 여부 확인·반영** — 사용자 요청으로 디맨드젠_전환캠페인 시트에서 "시즌5_13화_토이스토리" 소재 블록을 직접 확인. 블록의 일별 데이터 행이 7/6(비용 10,298원)에서 끝나고 7/7 행 자체가 없음(공란) — 장송의프리렌이 정확히 7/6부터 투입된 시점과 맞물려, 토이스토리가 7/6부로 종료된 것으로 확인. 테이블에 이미 표시돼 있던 7월 누적 수치(노출 63,541·클릭 153·광고비 68,168원)는 7/1~7/6 실측과 소수점 단위까지 정확히 일치해 그대로 유지하고, "운영기간"만 "6/9 ~ 현재" → "6/9 ~ 7/6"으로, 상태를 "활성" → "중단"(마이클잭슨·야구와 동일한 OFF 캠페인 취급)으로 수정, 행 순서도 활성 소재 뒤·다른 중단 소재 앞으로 재배치. **주의**: 이 테이블(전환캠페인 소재별)은 `update_dashboard.ps1`이 아직 전혀 건드리지 않는 100% 수동 영역이다 — 다음에 소재 교체가 또 있으면 이번처럼 직접 Excel 블록의 마지막 데이터 행을 확인해서 종료 여부를 판단해야 한다.

---

## 참고: 주요 시트 구조 요약

| 시트명 | 일별총합 6월합계 | 소재별 6월합계 |
|--------|----------------|--------------|
| 디맨드젠_클릭최대화 | R69~(행 확인 필요) | R104 (C4/C14/C24=비용) |
| 디맨드젠_후속조회 | R19 (C4=비용) | R55 (C4/C14/C24=비용) |
| 디맨드젠_전환캠페인 | R184 (C4=비용, C12=전환) | R377 (C4/C14/C24=비용) |
| HOT&NEW_조회 | R97~ | R163~(회차별) |
| Grand total | R16 = 유튜브 subtotal | C6=노출, C7=조회, C8=클릭, C12=소진금액(VAT+) |

## 참고: KPI 컬러 클래스 규칙

| class | 색상 | 사용 지표 |
|-------|------|----------|
| `kpi a` | orange | 광고비·집행액·CPC (비용·효율 지표) |
| `kpi t` | teal | 조회수·CTR·CPV·CVR·전환건수 (긍정 성과 지표) |
| `kpi` | 기본 | 노출수·기타 참고 지표 |

## 참고: 파일 위치

```
<프로젝트 루트>/                                           ← git clone 위치 (Claude Code 작업 디렉토리)
├── 차이_한화_라이프플러스_라플TV_Report_6월_26MMDD.xlsx   ← 엑셀 원본 (팀 내부 비밀번호)
└── 라플TV_캠페인_대시보드.html                            ← 업데이트 대상 대시보드
```
